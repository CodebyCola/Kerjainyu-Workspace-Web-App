# Get this from Supabase → Project Settings → Database → Connection string (URI)
# Use the "Transaction" pooler string if deploying serverless functions,
# or the direct connection string if running a long-lived Express server.
DATABASE_URL=postgresql://postgres:[PASSWORD]@[HOST]:5432/postgres
